---
name: huatuo
disable-model-invocation: true
author: 本地自研
description: |
  华佗 huatuo：多视角深度审计一个技能或技能库：确定性 lint 先行（audit_lint.py：坏链接/孤儿/token预算/模糊词/路径硬编码）+ 派独立子代理从结构/流程/数据/文档/契约同步视角读实际文件找缺陷，反证门拦截假阳性，产出带文件行号的问题清单 + findings.json schema 验证 + 修复闭环。内置大野耐一视角准则（现地现物/七大浪费/五个为什么/防错）。
  触发：审计技能、看看这技能写得怎么样、审一下这个技能、技能审计、检查一下技能库、这个技能有什么问题。
  不触发：代码实现审查（→ bianque 扁鹊）；技能评估打分与优化（→ darwin-skill）；单文件小改动（→ 直接改）；装第三方技能的安全体检（→ skill-vetter）。
---

# 华佗 Huatuo · 多视角技能审计

**核心心智**：审计是「找缺陷」不是「打分」——问题必须带现场证据（文件名+行号+原句），修复必须闭环（改完跑回归）。与 darwin-skill 分工：本技能审（找问题）、darwin 优（改质量）。

## 大野式审计准则（内嵌视角，详见 ohno-taiichi-perspective）

| # | 准则 | 审计动作 |
|---|------|---------|
| 1 | **现地现物** | 只读实际文件/跑实际命令，不采纳「文档里写了」的转述；文档与实现不一致，以实现为准 |
| 2 | **七大浪费** | 对照查：重复实现=过量生产、孤儿文档/死参数=库存、静默吞错=缺陷、流程断点=等待、路径漂移=搬运 |
| 3 | **五个为什么** | 每个问题追问根因到系统缺陷（不是个人失误）；「改掉」不算完，「为什么产生、为什么没拦住」才算 |
| 4 | **标准+防错** | 先查标准（协议/模板/命名约定）是否存在且还在修订；再查防错装置（幂等/防重/损坏告警）；检测不阻断=没有防错 |
| 5 | **自働化（异常即停）** | 脚本静默吞错=伪装成成功的失败，最高优先修；每个修复配「防再发」机制，不给「下次注意」 |

## 芒格式逆向审计（引用 charlie-munger-perspective，审计启动时先问）

在审计前基线里加两问（逆向思维 + 能力圈）：
- **逆向**：这个技能「怎么用会出事」？从失败模式反推缺陷（比正向找问题命中率高）
- **能力圈**：审计者自己承认不懂的领域（如平台规则/领域知识）→ 标注「此维度未审，需领域专家」，不装内行

## 费曼式可解释性审计（引用 feynman-perspective，审计文档时加）

对每条规则/文档段落跑三条可机械判定的测试：
- **定义剥夺测试**：禁掉术语后还能用大白话重述吗？重述不出=术语堆砌（cargo cult）
- **大一新生测试**：第一次看的 agent 能照做吗？不能=没写清
- **最小例子测试**：抽象规则有具体例子吗？给不出=空话

## 执行流程

1. **审计前基线**（🔴 先答再开审）：写清审计对象，模板照抄填空：
   - 触发词：{该技能会被哪些话触发}
   - 目标读者：{谁在什么 runtime 用它}
   - 预期行为：{正常触发后应产出什么}
   - 边界：{它明确不做什么}
   - 好的标准：{一句话成立条件，例：「跑完能给出带证据的缺陷清单且不误伤」——写不出 = 审计没有标尺}
   没有基线的缺陷判定是「为挑刺而挑刺」。
2. **确定性 lint 先行**：跑 `{SKILL_DIR}/scripts/audit_lint.py <技能目录>`（零 LLM，判据见 `references/lint-checks.md`）。L1 坏链接/L2 孤儿/L6 无关文件/L7 残留/L8 路径硬编码的命中直接进清单；L3 token 预算/L4 模糊词/L5 跨语言污染作为子代理的待判断输入。**lint 能查的不浪费子代理**。
3. **定视角**：按审计对象结构选 3-4 个视角。技能库标准四视角：主线流程 / 脚本与数据 / 编排路由 / 质量闭环；单一技能用：结构 / 流程 / 文档 / 闭环。涉外部输入/下载/凭据的技能加第 5 视角：**安全对抗**（提示注入/数据外泄/供应链，SkillSpector 17 类风险思路）。有「权威路径脚本（如流程仪表盘/生成器）」或「脚本间字段往来」的技能加：**契约同步**（5 个维度清单见 `references/audit-protocol.md` 视角 G）。
4. **派子代理（冷启动）**：每视角一个独立子代理（后台并行），协议模板见 `references/audit-protocol.md`。**任务预算**（防子代理超时，2026-08-16 实战：多文件精读+长输出型子代理 5/5 超时、小而精任务全成）：每视角限读 2-3 个文件、候选缺陷输出上限 15 条（超时降级处理见失败模式第 2 条）。**冷启动纪律**：子代理之间不共享结论（多代理辩论会放大偏差，Judging with Many Minds 实证）；每个候选缺陷必须带「文件+行号+原句+触发方式（怎么验证它存在）」，证据不全的不算缺陷。
4a. **跨段语义扫描（主线程，切片派发的补偿义务）**：切片/预算让子代理只能看到窗口内内容，**跨段缺陷是子代理的结构性盲区**——主线程必须自己扫一遍全文补这一层：① 跨段矛盾（同一概念两处定义打架，如「触顶」两义、90 行 vs 103 行自相矛盾）② 悬空引用（段内「见下方/见 Step N」指向段外不存在处）③ 术语一致性（同一概念多个名字）④ 流程衔接（上步产出下步不消费）⑤ **主文件 ↔ 下沉文件规则一致性**（主文件与 references 里同一规则/格式/数字必须同文对照 grep，如 `grep -rn '11 条 fallback' SKILL.md references/` 双处核对——2026-08-16 实战：32 条修复改了主文件规则，5 个下沉文件 7 处旧规则未同步，全由本维拦截）。lint 的全局检查（L1/L2/L8）只兜机械项，语义层靠本步。
5. **反证阶段（假阳性拦截门）**：候选清单出来后，派一个独立反证子代理逐条尝试推翻——读同一文件验证证据是否真实、问题是否成立（反证同样按切片派发：给行号清单 + 单行 sed 验证，不给全文；输出格式逐条一行「N. 成立|误报——一句理由」，末尾一行「推不翻数：X / 推翻数：Y」）。**推不翻的才进最终清单**（Refute-or-Promote：79-83% 候选是「看似合理但站不住」，不设此门清单会被假缺陷淹没）。
6. **主线程仲裁排序**（🔴 检查点，修复前给用户看）：主线程做 meta-judge，不简单拼观点。按 SonarQube 五级 rubric 定级：
   - **Blocker**：技能无法被正确触发/会产出错误指令/数据损坏（立即修）
   - **High**：关键环节缺失、检测不阻断、静默吞错、安全风险
   - **Medium**：幂等缺口、重复实现、数据正确性风险
   - **Low**：文档漂移、触发词缺口、代码卫生
   - **Info**：可选优化
   交叉印证（≥2 视角命中）升级一档。**严重度校准规则**（吸收自 multi-reviewer-patterns）：可被外部利用的安全问题（注入/越权/密钥泄露）无论几票命中一律 Blocker 起步，不因「仅 1 视角报」而降级。输出：问题 / 证据 / 定级 / 修复方案。**报告纪律**：结论一句话讲清；证据落到文件+行号+原句（含「在 X 条件下会 Y」）；末尾必写「反方证据」（支持「这段没问题」的证据）；不写「可能存在X问题」式含糊句。同时落一份机器可查的 `findings.json`（schema 见 `references/findings-schema.json`），跑 `{SKILL_DIR}/scripts/validate_findings.py findings.json` 验证四要素齐全——散文报告给用户，JSON 给 CI。
7. **修复闭环**：按 Blocker→High→Medium→Low 顺序修；每修一处跑回归（有 tests 跑 tests，没有跑最小触发验证）。
8. **回归验收（二次审计）**（🔴 修复完成后给用户看验收结果再收尾）：修复后重跑同一份检查清单验证三项——(a) 原缺陷消除 (b) **无新引入缺陷**（2026-08-16 实战：32 条修复引入 7 处主文件↔下沉文件漂移 + 账本旧格式未迁移，机械 lint 全绿但语义漂移漏过——二次审计必须重跑四层清单，不只跑 lint）(c) 触发词回归通过。**规则变更 → 存量数据迁移检查**：凡是修复动了格式/枚举/字段/阈值定义，必须 grep 存量数据（账本/示例文件/下沉文件）确认同步迁移，未迁移=修复未完成。三项都过才算闭环完成（SonarQube clean-as-you-code / KCS Evolve Loop）。**事实断言独立复核**（cloudflare security-audit 第 6 阶段）：最终清单里每条「已修复」的证据（行号/原文）由主线程重读一次文件确认，不信「修好了」的转述。
9. **收尾**：审计结论一句话 + 修复数 + 回归结果；发现的新缺陷模式登记进审计对象自己的 issue-patterns.md（若其有）。

## 失败模式

- 子代理不可用 → 降级串行：一个视角做完落盘再做下一个，不挂起等通知
- 子代理超时（3 轮未归即判超时；2026-08-16 实证：会话后期代理会拖到第 3 轮才交票，2 轮阈值误杀）→ 中断，改主线程内联执行，并在报告标注「inline 降级，置信度低于独立子代理」。**会话卫生**：单会话累计 80+ 子代理后全局退化（bash 通道也挂），长任务应分会话跑
- 审计对象无文档（纯脚本库）→ 以代码为现场，文档维度改为「脚本间调用关系」
- 审计对象很大 → 视角拆分更细：**每视角限读 2-3 个文件**（实战教训：限 8-10 个时多文件精读型子代理会超时，2-3 个是小而精的安全区）；大对象用分层抽样（按文件类型/视角分层），在结论里声明覆盖率 X% 与置信度
- 判断型子代理（视角/反证）一律收**预筛行段清单**（不限主文件大小）：主线程先用确定性 lint + 结构化 grep 产出可疑行号，子代理只对指定行段（≤30 行/段）做判断，候选 ≤10 条（2026-08-16 隔离实验矩阵定论：机械任务任意大小秒回；判断任务在 19 行小文件成功、299/512 行全挂）
- 行段交付方式强制用 bash 一次性取内容（`sed -n 'X,Yp'`），**禁止子代理用 Read 工具读全文**——边界探针定论：sed 取内容+判断在 200 行内稳定归回；Read 全文+判断 15/15 超时。机制推断：Read 大文件诱发「反复回读取证」循环卡死，bash 一次性交付无此循环
- 子代理结论冲突 → 保留矛盾，标注「两视角结论不一致」交用户判断
- 反证子代理也推不翻但主线程仍存疑 → 标注「未定级，需人工复核」，不算通过
- `audit_lint.py` 报「不是技能目录」（exit 2）→ 两查：对象是纯脚本库？走「无文档」分支；路径给错了？改路径重跑
- `validate_findings.py` 未通过 → 按报错补四要素重新生成 findings.json，**不许跳过验证直接交报告**；重试 2 次仍不过 → 报告里如实标注「结构化输出未验证」
- findings.json 生成不了（对象太大/审计中断）→ 允许先交散文报告，findings.json 必须在回归验收（第 8 步）前补齐并通过验证
- lint 崩溃/规则文件损坏 → 引擎自带降级提示（跳过该检查），审计照跑，但结论声明「确定性检查未完成，覆盖率降级」

## 边界声明（诚实边界）

- **静态扫描 ≠ 安全边界**：lint/确定性检查抓的是质量与结构缺陷；安全对抗（提示注入/供应链/凭据）必须走视角 E + skill-vetter，不得用 lint 通过代替安全审查（skillscan-lint 退役公告的实证结论：静态扫描不能当技能安全边界）。
- 本技能审的是「技能本身」（结构/流程/契约/文档），不是「技能写的代码」——代码审查另走 bianque（扁鹊）。
- 审计者能力圈外（平台规则/领域知识）必须标注「此维度未审」，不装内行（芒格准则）。

## 不要做

- 不给技能打分（darwin-skill 的活）；只报问题与证据
- 不顺手重构——审计与修复分开，修复另起一轮
- 不采纳「听说/感觉」类无证据结论；证据不全的候选直接丢弃，不补证据
- 不在 SKILL.md 里写死某个具体技能的审计结果（结论属于那本书/那个仓库）
- 不让视角子代理互相看结论（冷启动）；汇总时主线程做仲裁，不简单叠加多数意见
